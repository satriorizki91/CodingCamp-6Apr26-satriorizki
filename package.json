{
  "name": "productivity-dashboard",
  "version": "1.0.0",
  "description": "A browser-based productivity dashboard with time tracking, tasks, and quick links",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "fast-check": "^3.15.0",
    "jsdom": "^23.0.0"
  }
}
